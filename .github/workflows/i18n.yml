name: i18n

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - 'components/**'
      - 'lingui.config.*'
  workflow_dispatch: {}

jobs:
  upload_sources:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello world"
